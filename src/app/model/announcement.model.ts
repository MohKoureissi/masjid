export interface Announcement {
  id: string|null;
  title: string;
  place: string,
  description: string;
  location: string;
  date: string;
  hour: string;
  imageUrl: string|null;
  organizer: string;
}
